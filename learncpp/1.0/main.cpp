// This is a preprocessor directive, which includes the standart input output c++ library.
#include <iostream>

// This is a function called 'main', which will need to get a returnable number by the end of the function.
int main() {
    // Uses the standart libary to print out 'Hello World!' in the console.
    std::cout << "Hello World!" << '\n';
    // As said previously, the function needs a returnable number by the end, and we return 0, which tells that everything ran fine.
    return 0;
}